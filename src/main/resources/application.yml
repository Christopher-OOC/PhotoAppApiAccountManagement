server:
  port: 0
spring:
  application:
    name: PhotoAppAPIAccountManagement
  devtools:
    restart:
      enabled: true
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8010/eureka
